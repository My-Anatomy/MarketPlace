// Placeholder for image upload helpers (S3 / Cloudinary)
export const uploadImage = async (fileBuffer, filename) => {
  // Implement your upload logic here and return the URL
  return `https://cdn.example.com/${filename}`;
};
